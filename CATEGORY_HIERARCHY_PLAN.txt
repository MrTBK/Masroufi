CATEGORY HIERARCHY PLAN — Masroufi (مصروفي)
Date: 2026-09-07 | Status: planned, not implemented

1. GOAL
- Add Primary Category → Subcategory relationships using parentId
  (e.g. Food & Drinks → Cafe / Restaurant / Groceries).
- Keep the dashboard structure and the existing icon system intact.
- Repair corrupted lib/core/analytics/insights.dart and
  test/analytics_test.dart and get `flutter analyze` back to 0 errors
  before considering this work complete.

2. AGREED DECISIONS (user-confirmed)
- Depth: single level only (parent → child, no grandchildren).
- Defaults: group existing 20 expense + 7 income defaults under new
  parents (migrate on upgrade, do not leave flat).
- Parent rules: free-form (any parent/child combo allowed;
  transactions allowed on parents too; parent totals = own txns +
  children rollup, counted once).
- Archive/delete: archiving a parent cascades to children; delete is
  blocked while children exist.

3. SCHEMA CHANGE (Drift/SQLite, v4 -> v5)
- tables.dart: add `TextColumn get parentId => text().nullable()()`
  to Categories (null = top-level parent). Plain-text ref, no FK
  (consistent with existing codebase: archiving never breaks history).
- app_db.dart: schemaVersion 4 -> 5; onUpgrade `if (from < 5)
  addColumn(categories.parentId)`; extend beforeOpen to run hierarchy
  seed/backfill when versionBefore < 5.
- Regenerate lib/data/database/app_db.g.dart via build_runner.

4. SEED + MIGRATION
- strings.dart: add ~4-6 parent nameKeys (e.g. cat_food_drinks,
  cat_transport, cat_home_bills, cat_lifestyle, plus 1-2 income
  parents) using icons already in CategoryIcons registry (no new
  icons) + categoryNames in en/fr/ar.
- Add child->parent map (existing nameKey -> parent nameKey).
- categories_repo.seedDefaults(): seed parents first (kind-matched,
  priority normal), then existing 20+7 children with parentId set;
  idempotent per nameKey; upgrade backfills parentId for pre-v5 rows
  by nameKey; user customs stay top-level (parentId null).

5. REPO RULES (icon system untouched)
- categories_repo.dart: create(..., {parentId}), setParent(id,
  parentId?), childrenOf(), roots(), tree() helpers.
- Validation: parent must exist and be top-level itself
  (parent.parentId == null); reject self-parent; single level only.
- setArchived cascades parent -> children; delete blocked when
  children exist (localized error).
- New pure rollup helper: parent total = own txns + children sums.
- CategoryIcons / CategoryVisuals: no changes; parents and children
  each store their own stable icon key.

6. UI (dashboard order kept)
- categories_page.dart: grouped sections — parent header row
  (CategoryAvatar + name + count) with expandable indented children;
  child dialog gains parent dropdown (top-level list + "No parent").
- txn_form_page.dart: category grid labels show "Parent › Child";
  stores selected id in existing categoryId field as today.
- history rows: leaf icon + "Parent › Child" subtitle.
- dashboard_page.dart: keep hero -> month -> budget -> upcoming ->
  recent -> top-cats -> actions; top-cats shows rolled-up parent
  totals (parent avatar, tap drills to filtered history).
- reports_page.dart / budget_page.dart: by-category aggregates roll
  up to parents; detail rows expandable; category-budget dialog
  accepts parent or leaf.
- strings.dart: add only hierarchy keys needed (subcategory,
  noParent, hasChildrenDeleteBlocked, parent names) in en/fr/ar.

7. BACKUP + TESTS
- backup_codec.dart v4 -> v5: include parentId in category JSON;
  accept v4 backups (missing parentId -> null, then backfilled).
- backup_page.dart export/import updated for parentId.
- migration_test.dart extended to v1 -> v5 (old rows parentId null,
  seeding assigns e.g. Food & Drinks -> cafe).
- ux_refinement_test.dart seed counts updated for new parents.
- New hierarchy_test.dart: single-level enforcement, self/cycle
  rejection, cascade archive, delete-blocked, rollup math, upgrade
  backfill.

8. REPAIRS (required for analyze -> 0)
- lib/core/analytics/insights.dart (currently zero-filled binary):
  rewrite as pure buildInsights({currentExpense, previousExpense,
  topCatName, funSharePct, incomeMoM, dailyAvgFormatted, lang})
  using AnalyticsStats + Strings.tpl for the 7 ins* templates;
  null-safe (skip on insufficient data). No new dependencies.
- test/analytics_test.dart (currently zero-filled binary): rewrite
  covering Periods (day/week mon-sun-sat/month/lastCompletedMonths),
  AnalyticsStats (average, MoM null-on-zero, partial, share),
  Insights.buildInsights (7 templates en + fr/ar spot-check,
  placeholder substitution, null-skipping), AnalyticsRepo
  (in-memory: sums exclude transfers, by-category, by-priority
  uncategorized->normal, monthlySeries, dailyExpenseTotals).

9. VERIFICATION (definition of done)
- `flutter analyze` -> 0 issues (today: 15990 illegal_character
  errors, all from the 2 corrupt files).
- `flutter test` -> all pass, including rebuilt analytics tests.
- Update MASROUFI_IMPLEMENTATION_LOG.md and docs/database.md
  (still says v1) to v5.

10. RISKS
- Grouping defaults changes seed counts and needs new localized
  parent names.
- Free-form + txns-on-parents needs careful rollup to avoid
  double-counting (covered by pure helper + tests).
- app_db.g.dart regen + providers.dart NUL-padding cleanup should
  ride along so the tree stays committable.

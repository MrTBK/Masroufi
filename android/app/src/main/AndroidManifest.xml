<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Biometric unlock (local_auth). Normal-level permission; the
         biometric prompt itself is OS-handled, no data leaves device. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Local alert notifications (runtime-granted on Android 13+).
         Inexact scheduling only: no exact-alarm permission needed. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Ads + cloud AI (opt-in): core finance works offline in airplane
         mode; only ads / AI-explain need network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <application
        android:label="Masroufi"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Widget "+" shortcut: cold start via the path-style URI
                 lands in the expense form through go_router's initial
                 route. Warm start (singleTop) opens home instead. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="masroufi" android:pathPrefix="/add" />
            </intent-filter>
        </activity>
        <receiver
            android:name=".MasroufiWidgetProvider"
            android:exported="false"
            android:label="Masroufi">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/masroufi_widget_info" />
        </receiver>
        <!-- Savings-goal variant (Track 7): subclass of the same Kotlin
             provider (shared onUpdate/layout/prefs) with a separate
             picker entry. A subclass is required: the manifest merger
             rejects two receivers with the same class name. -->
        <receiver
            android:name=".MasroufiSavingsWidgetProvider"
            android:exported="false"
            android:label="Masroufi Savings">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/masroufi_savings_widget_info" />
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- AdMob App ID: test ID by default; replace via manifest
             placeholder or --dart-define ADS_APP_ID at release. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
